// https://open.kattis.com/contests/ccpc18-practice/problems/carrots

#include <bits/stdc++.h>
using namespace std;

int main() {
    int n, p; string s;
    scanf("%d %d", &n, &p);
    while (n--) {
        cin >> s;
    }
    printf("%d\n", p);
}